/**
 * This package contains the query model(s).
 * <p>
 * This also belongs to the core of the application, even if it "only" contains the projections.<br>
 * It should only use "boundary" package services to communicate.<br>
 * It should be as decoupled as possible from libraries, frameworks, infrastructure,...
 */
package io.github.joht.showcase.quarkuseventsourcing.query.model;