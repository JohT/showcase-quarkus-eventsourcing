# Ignore git settings for expport
.gitattributes export-ignore
.gitignore export-ignore
#
# Handle line endings automatically for files detected as text. Performs LF normalization.
* text=auto
#
# Explicitly declare text files we want to always be normalized and converted
# to native line endings on checkout.
*.java          text diff=java
*.html          text diff=html
*.properties    text
*.xml           text
*.txt           text
*.md            text
*.css           text
*.js            text
*.sql           text
*.ddl           text